{
    "name": "@tommasomeli/prisma-generator-nestjs-dto-example-blog",
    "version": "0.0.0",
    "private": true,
    "description": "End-to-end example of @tommasomeli/prisma-generator-nestjs-dto: configFile, custom @Auditable annotation, and AuditGenerator plugin.",
    "scripts": {
        "gen": "prisma generate",
        "gen:clean": "rm -rf generated && npm run gen"
    },
    "dependencies": {
        "@prisma/client": "^7",
        "@tommasomeli/prisma-generator-nestjs-dto": "file:../..",
        "prisma": "^7"
    },
    "devDependencies": {
        "typescript": "^5.7.3"
    }
}
